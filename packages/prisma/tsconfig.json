{
  "extends": "tsconfig/typescriptService.json",
  "include": ["."],
  "exclude": ["dist", "build", "node_modules"],
  "compilerOptions": {
    "declaration": false, // this is temporary while waiting for a prisma fix
    "declarationMap": false // this is temporary while waiting for a prisma fix
  }
}
